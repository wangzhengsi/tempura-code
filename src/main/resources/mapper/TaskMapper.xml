<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tempura.code.order.mapper.TaskMapper">

    <!--数据插入-->
    <insert id="insert">
        INSERT INTO `task` (`task_content`) VALUES (#{taskContent})
    </insert>
</mapper>